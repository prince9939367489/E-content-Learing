# Leave blank to use Vite's /api development proxy.
# In production, include the /api prefix, for example https://api.example.com/api.
VITE_API_URL=
# Set true only for the browser-only portfolio demo build.
VITE_DEMO_MODE=false
